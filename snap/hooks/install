#!/bin/bash

logger "hook: $(basename $0): hook start"

# run common part of the hook
${SNAP}/bin/snap-hooks-common

# disable reindex helper service
snapctl stop --disable ${SNAP_INSTANCE_NAME}.rebuild-index-helper
