#!/bin/bash

if [ "$($SNAP/usr/bin/whoami)" != "root" ]; then
    echo "Please run as root!!"
    exit 0
fi

source $SNAP/bin/read-settings

pushd $SNAP_COMMON
action=$1
shift
exec "$SNAP/var/opengrok/bin/${action}" "$@"
popd
